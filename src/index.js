import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// renders the App component containing all app components
ReactDOM.render(<App />, document.getElementById('root'));
